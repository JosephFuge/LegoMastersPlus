@model IEnumerable<string>

<div class="d-grid gap-2">
    <a class="btn btn-outline-secondary" asp-route-productCategory=""
       asp-route-pageNum="">Products</a>

    @foreach (string pt in Model ?? Enumerable.Empty<string>())
    {
        <a>@pt</a>
    }
</div>
